#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - The entrypoint to the program
 *
 * Return: Always (0)
 *
 */

int main(void)
{

	int a;

	for (a = 0; a <= 9; a++)
	putchar('0' + a);
	putchar('\n');

	return (0);
}
